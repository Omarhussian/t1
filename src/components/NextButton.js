import React from 'react'
import Button from '@material-ui/core/Button';

const Next = () => {
    return ( 

        <Button  
        variant="contained" 
        color="primary" 
        size="large"
        className="next-button"
        >
            Next
        </Button>
    
    );
}
 
export default Next;