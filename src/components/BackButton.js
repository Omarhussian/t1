import React from 'react'
import Button from '@material-ui/core/Button';

const Back = () => {
    return ( 

        <Button
     variant="contained"
      color="secondary"
      size="large"
      className="back-button"
      >
            Back
        </Button>
    
    );
}
 
export default Back;